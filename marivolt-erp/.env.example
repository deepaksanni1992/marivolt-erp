VITE_API_BASE=http://localhost:5000
VITE_API_BASE_URL=http://localhost:5000

# Document uploads use POST /api/documents/upload on the backend (AWS S3). Configure AWS only on the server (.env in backend/).
