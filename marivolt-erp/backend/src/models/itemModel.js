/**
 * Canonical Item (article) model — primary key is `itemCode` (Article).
 * Implementation lives in `Item.js`; this file matches the documented module layout.
 */
export { default } from "./Item.js";
